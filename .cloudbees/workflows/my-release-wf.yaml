apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: my-release-wf
on:
  workflow_dispatch:
    inputs:
      manifest:
        type: string
        required: true
        description: See doc link to application release manifest
metadata:
  stages/v1alpha1:
    - name: dev
      jobs:
        - Deploy-to-dev
    - name: qa
      jobs:
        - Deploy-to-qa
    - name: prod
      jobs:
        - Deploy-to-prod
jobs:
  Deploy-to-dev:
    needs: []
    steps:
      - run: |
          echo "hello world"
        uses: docker://golang:1.20.3-alpine3.17
        name: deploy
        shell: sh
  Deploy-to-qa:
    needs:
      - Deploy-to-dev
    steps:
      - run: echo "hello world"
        uses: docker://golang:1.20.3-alpine3.17
        name: deploy
        shell: sh
  Deploy-to-prod:
    needs:
      - Deploy-to-qa
    steps:
      - name: deploy
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: echo "hello world"
