apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: app-props-test

on:
  workflow_dispatch:

jobs:
  build:
    steps:
      - name: Props test
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "App property app_prop1: ${{vars.app_prop1}}"
          
